---
import { getCollection } from "astro:content";
import AwardCard from "./Card.astro";

const awards = (await getCollection("awards")).sort(
  (a, b) => b.data.year - a.data.year
);
---

<section class="py-12">
    <h2 class="text-3xl font-bold mb-8 text-center">Awards</h2>
    <div class="grid grid-cols-1 sm:grid-cols-3 md:grid-cols-4 gap-3 w-fit">
      {awards.map((award) => (
        <AwardCard entry={award} />
      ))}
    </div>
</section>